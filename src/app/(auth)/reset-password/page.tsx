import {AuthLayout} from "@/components/auth-layout";import {ActionForm} from "@/components/action-form";import {resetPassword} from "../login/actions";
export default function ResetPassword(){return <AuthLayout><div className="kicker">Acceso seguro</div><h1>Tu nueva contraseña</h1><p>Elige una contraseña que no utilices en otros servicios.</p><ActionForm action={resetPassword} className="space-y-5"><label className="block text-sm">Nueva contraseña<input name="password" className="input mt-2" type="password" minLength={10} maxLength={128} autoComplete="new-password" required/></label><label className="block text-sm">Repite la contraseña<input name="confirmation" className="input mt-2" type="password" minLength={10} maxLength={128} autoComplete="new-password" required/></label><button className="btn btn-primary w-full">Guardar contraseña</button></ActionForm></AuthLayout>;}
