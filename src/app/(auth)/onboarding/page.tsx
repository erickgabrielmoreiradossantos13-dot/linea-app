import {AuthLayout} from "@/components/auth-layout";import {ActionForm} from "@/components/action-form";import {createOrganizationAction} from "./actions";import {isDemoMode} from "@/lib/env";import {redirect} from "next/navigation";
export default function OnboardingPage(){if(isDemoMode)redirect("/dashboard");return <AuthLayout><div className="kicker">El primer paso</div><h1>Tu negocio empieza aquí.</h1><p>Crea tu organización. Después podrás añadir contactos, conectar tu web y trabajar con tu equipo.</p><ActionForm action={createOrganizationAction} className="space-y-5"><label className="block text-sm">Nombre de tu negocio<input className="input mt-2" name="name" minLength={2} maxLength={160} required placeholder="Ej.: Línea Sur Digital Studio"/></label><button className="btn btn-primary w-full">Crear mi espacio</button></ActionForm></AuthLayout>;}
